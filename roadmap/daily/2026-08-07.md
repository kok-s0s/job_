# 2026-08-07：第 1 阶段集成验收

今天是 2026-08-07，周五。按 [工作日 2 小时量化学习计划](/roadmap/weekday_2h_plan)，今天对应 **第 4 周周五**。

## 今日目标

今天不新增大功能，重点是把第 1 阶段产出的 ROS2 Runtime Demo 做一次完整集成验收：确认多节点能启动，Topic / Service / Action 能闭环，状态机、结构化日志、心跳、watchdog 和性能指标都能被一条脚本验证。

今天完成后，你要能讲清楚：

- 这个 demo 由哪些节点组成，每个节点负责什么。
- Topic、Service、Action 在这个系统里分别解决什么问题。
- 状态机如何从正常任务、故障、恢复链路里保护系统。
- 心跳、watchdog、性能指标如何证明系统“可观测、可诊断、可复盘”。

## 2 小时安排

| 时间 | 任务 | 产出 |
|------|------|------|
| 20 分钟 | 复盘第 2-4 周能力链路 | 画出节点、Topic、Service、Action 清单 |
| 80 分钟 | 在 ROS2 环境执行完整验收脚本 | 留下 `[ok]` 输出和关键日志字段 |
| 20 分钟 | 写集成验收复盘和面试表达 | 1 段项目讲解 + 1 张验收清单 |

## 今日背景

第 1 阶段已经完成这些能力：

```txt
Topic: /robot/imu, /robot/joint_states, /runtime/heartbeat
Service: /runtime/query_status, /runtime/reset_fault, /runtime/apply_event
Action: /runtime/execute_task
Node: sensor_sim_node, runtime_node, heartbeat_monitor_node, watchdog_node
```

今天要做的是“把它们当成一个小系统验收”，而不是只看某个节点单独能跑。真实机器人软件面试里，能把链路讲完整，比单点 API 更有说服力。

## 练习题

### 题目 1：整理系统拓扑

写出下面这张表：

| 模块 | ROS2 接口 | 作用 | 验收证据 |
|------|----------|------|----------|
| 传感器模拟 | `/robot/imu`、`/robot/joint_states` Topic | 持续发布底层数据 | `ros2 topic echo` 和 `topic hz` |
| 运行时节点 | 订阅传感器 Topic，提供 Service / Action | 维护状态机和任务执行 | `/runtime/query_status`、Action result |
| 心跳监控 | `/runtime/heartbeat` Topic | 记录节点是否活着 | `[heartbeat_table]` |
| watchdog | `/runtime/heartbeat` + `/runtime/apply_event` | 心跳超时触发 Fault | `[watchdog] status=OK/TIMEOUT` |

验收：能说明每个模块的输入、输出和失败时会影响哪里。

### 题目 2：执行完整 ROS2 验收

在 ROS2 环境下执行：

```bash
cd projects/ros2_runtime_demo
bash scripts/verify_ros2_runtime_demo.sh
```

预期最终输出：

```txt
[ok] ROS2 runtime demo verified: state machine demo, week 3 review, structured runtime_log fields, heartbeat pub/sub, watchdog timeout check, performance metrics, typed topics, runtime health, fault metadata, apply_event/query/reset services, and execute_task Action completion/rejection/cancellation are working
```

验收：脚本退出码为 `0`，并能看到最终 `[ok]` 行。

### 题目 3：抽取 6 类关键证据

从脚本输出或日志中找出这 6 类证据：

```txt
[ok] runtime state machine transitions verified
[runtime_log] ... event=heartbeat ...
[heartbeat_table] node=runtime_node ... status=OK
[watchdog] node=runtime_node status=OK ...
[perf] node=runtime_node ... max_callback_duration_ms=...
Result: success: true message: task completed
```

验收：每类证据都要能说出“它证明了系统哪部分工作正常”。

### 题目 4：写一段第 1 阶段项目表达

准备一段 1 分钟表达：

```md
我做了一个 ROS2 机器人运行时 Demo，用多节点模拟机器人系统的基础运行链路。传感器节点通过 Topic 持续发布 IMU 和关节状态，runtime 节点订阅后维护状态机，并通过 Service 暴露状态查询、故障复位和外部事件注入；长耗时任务用 Action 实现，支持 feedback、result 和 cancel。为了让系统可诊断，我加了统一 runtime_log、节点 heartbeat、watchdog 超时检测和性能指标输出。最后用一个 verify 脚本把构建、launch、Topic、Service、Action、状态机、故障恢复和监控指标全部串起来验收，确保它不是零散功能，而是一个能闭环运行的小型机器人运行时骨架。
```

验收：能用自己的话讲出来，重点放在“系统闭环”和“可观测性”。

### 题目 5：列出下一阶段入口

第 5 周进入 DDS QoS 与通信可靠性。今天先留下 3 个问题：

```md
1. 高频传感器数据为什么常用 best effort？
2. 控制命令和状态切换为什么更适合 reliable？
3. queue depth 变大时，延迟和丢包风险会如何变化？
```

验收：能把这 3 个问题和当前 demo 里的 `/robot/imu`、`/runtime/apply_event`、`/runtime/heartbeat` 对应起来。

## 今日验收标准

- [x] 写出系统拓扑表。
- [x] 在 ROS2 环境执行完整验收脚本。
- [x] 记录最终 `[ok]` 输出。
- [x] 找到 6 类关键日志/命令证据。
- [x] 写出 1 分钟第 1 阶段项目表达。
- [x] 留下第 5 周 QoS 学习入口问题。
- [x] `runtime_integration_review` 输出阶段验收拓扑、证据清单、项目表达和 QoS 入口问题。
- [x] `verify_ros2_runtime_demo.sh` 纳入第 1 阶段集成复盘程序检查。
- [x] 网页构建通过：`npm.cmd run build`。
- [x] ROS2 完整验收脚本通过。

## 已实现内容

- 新增 `runtime_integration_review.cpp`，作为第 1 阶段集成验收复盘程序。
- `runtime_integration_review` 输出 6 个系统模块/接口/职责/验收证据。
- `runtime_integration_review` 输出 6 类关键证据，覆盖状态机、结构化日志、心跳表、watchdog、性能指标和 Action result。
- `runtime_integration_review` 输出 1 段第 1 阶段项目表达，方便复盘和面试口述。
- `runtime_integration_review` 输出第 5 周 DDS QoS 的 3 个入口问题。
- `CMakeLists.txt` 已编译并安装 `runtime_integration_review`。
- `verify_ros2_runtime_demo.sh` 已运行并检查 `runtime_integration_review` 输出。
- `verify_ros2_runtime_demo.sh` 将正常 Action 验收任务调整为 30 步，确保 `/runtime/query_status` 能稳定命中 `state=RUNNING task_state=RUNNING`。

## ROS2 验收命令

```bash
cd projects/ros2_runtime_demo
bash scripts/verify_ros2_runtime_demo.sh
```

预期最终输出：

```txt
[ok] ROS2 runtime demo verified: state machine demo, phase 1 integration review, week 3 review, structured runtime_log fields, heartbeat pub/sub, watchdog timeout check, performance metrics, typed topics, runtime health, fault metadata, apply_event/query/reset services, and execute_task Action completion/rejection/cancellation are working
```

## 今日复盘模板

```md
## 今日复盘

- 今天完成：
- 最能证明系统闭环的输出：
- 我现在能讲清楚：
- 第 1 阶段最大的收获：
- 下周第一条命令：
```

下周第一条命令建议：

```bash
cd projects/ros2_runtime_demo && bash scripts/verify_ros2_runtime_demo.sh
```

## 今日复盘

- 今天完成：把第 1 阶段集成验收做成可执行复盘程序，并接入 ROS2 自动验收脚本。
- 最能证明系统闭环的输出：最终 `[ok]` 行同时包含 `phase 1 integration review`、Topic、Service、Action、heartbeat、watchdog 和 performance metrics；Action 执行期间 query 能看到 `state=RUNNING task_state=RUNNING`。
- 我现在能讲清楚：ROS2 Runtime Demo 不是零散节点，而是传感器流、状态机、故障恢复、长任务和可观测性组成的小型机器人运行时骨架。
- 第 1 阶段最大的收获：会用一条脚本证明系统能构建、启动、通信、恢复、监控和复盘。
- 下周第一条命令：`cd projects/ros2_runtime_demo && bash scripts/verify_ros2_runtime_demo.sh`

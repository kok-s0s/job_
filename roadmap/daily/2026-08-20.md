# 2026-08-20：固定触发一次故障复现

今天是 2026-08-20，周四。按 [工作日 2 小时量化学习计划](/roadmap/weekday_2h_plan)，今天对应 **第 6 周周四**。

## 今日目标

昨天已经给日志和心跳加上 session id。今天要把“能复现问题”落成脚本：固定启动 demo、固定 session id、固定触发一次 `SensorTimeout`，检查系统进入 `FAULT`，再走 `ResetFault -> RecoveryDone` 回到 `STANDBY`。

今天完成后，你要能讲清楚：

- 故障复现脚本为什么比手动点命令更适合项目验收。
- `SensorTimeout` 如何通过公开 `/runtime/apply_event` Service 触发，而不是绕过状态机。
- `query_status` 如何验证故障元数据：`state=FAULT`、`runtime_error=SENSOR_TIMEOUT`、`runtime_severity=CRITICAL`。
- session id 如何把故障触发、日志、心跳和恢复链串起来。

## 2 小时安排

| 时间 | 任务 | 产出 |
|------|------|------|
| 20 分钟 | 确认故障路径 | `SensorTimeout -> FAULT -> ResetFault -> RecoveryDone -> STANDBY` |
| 80 分钟 | 写自动复现脚本 | `scripts/verify_fault_reproduction.sh` |
| 20 分钟 | 跑脚本并同步网页 | `[ok] deterministic fault reproduction verified` |

## 已实现内容

- 新增 `runtime_fault_reproduction_review`，输出固定故障路径、证据点和面试总结。
- 新增 `scripts/verify_fault_reproduction.sh`，自动构建、启动 demo、设置 `ROBOT_RUNTIME_SESSION_ID`、触发故障、验证恢复。
- `verify_ros2_runtime_demo.sh` 已纳入 fault reproduction review 检查。
- 故障复现脚本会检查 `session_id` 是否出现在 query、runtime log、heartbeat 和 perf 输出中。

## ROS2 验收命令

```bash
cd /mnt/d/Work/Code/job_/projects/ros2_runtime_demo
bash scripts/verify_fault_reproduction.sh
```

关键输出：

```txt
[fault_reproduction] deterministic fault path
  step=inject command="ros2 service call /runtime/apply_event ... {event: SensorTimeout}" expected="accepted=True transitioned=True current_state='FAULT'"
  step=verify command="ros2 service call /runtime/query_status ..." expected="success=False state=FAULT runtime_error=SENSOR_TIMEOUT runtime_severity=CRITICAL"
  step=recover command="ResetFault then RecoveryDone" expected="state=STANDBY runtime_error=NONE"

[ok] fault reproduction review ready
[ok] deterministic fault reproduction verified: session_id=fault_repro_2026_08_20, SensorTimeout -> FAULT, ResetFault -> RecoveryDone -> STANDBY
```

## 今日验收标准

- [x] 固定触发 `SensorTimeout`。
- [x] 验证进入 `FAULT` 且错误码为 `SENSOR_TIMEOUT`。
- [x] 验证 `runtime_severity=CRITICAL`。
- [x] 验证恢复链 `ResetFault -> RecoveryDone -> STANDBY`。
- [x] 验证同一 session id 出现在 query、runtime log、heartbeat 和 perf 输出中。
- [x] ROS2 故障复现脚本通过。
- [x] 网页构建通过：`npm.cmd run build`。

## 今日实验记录

- 故障入口：`/runtime/apply_event` + `SensorTimeout`。
- 故障证据：`state=FAULT runtime_error=SENSOR_TIMEOUT runtime_severity=CRITICAL`。
- 恢复证据：`ResetFault` 进入 `RECOVERY`，`RecoveryDone` 回到 `STANDBY`。
- 可运行入口：`scripts/verify_fault_reproduction.sh`。
- 我现在能解释：故障复现脚本把“我能处理异常”变成可重复验证的工程证据。
- 明天第一条命令：`grep -R "rosbag\\|session_id\\|fault reproduction" -n projects/ros2_runtime_demo roadmap/daily/2026-08-*.md`。
